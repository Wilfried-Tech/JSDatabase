export enum DataType {
  Number = "number",
    String = "string",
    Boolean = "boolean",
    Date = "date",
    Object = "object",
    Array = "array",
    JSON = "json"
}

export enum ERROR {
  IDB_UNSUPPORTED = "idb_not_supported"
}

export enum Method{
  
}